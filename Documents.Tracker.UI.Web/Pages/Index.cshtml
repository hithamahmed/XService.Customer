@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

    @*<div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
        <div class="center">
            <div class="pagination">
                @if (Model._pager.EndPage > 1)
                {
                    @if (Model._pager.CurrentPage > 1)
                    {

                        <a>First</a>
                    }

                    @for (var page = Model._pager.StartPage; page <= Model._pager.EndPage; page++)
                    {
                        var p = page;

                        <a asp-route-page=@p class="@(page == Model._pager.CurrentPage ? "active" : "")">@p </a>
                    }

                    @if (Model._pager.CurrentPage < Model._pager.TotalPages)
                    {
                        <a  asp-route-page="@(Model._pager.CurrentPage + 1)">Next</a>
                        <a  asp-route-page="@(Model._pager.TotalPages)">Last</a>
                    }

                    @if (Model._pager.CurrentPage > 1)
                    {
                        <a  asp-route-page="@(Model._pager.CurrentPage - 1)">Previous</a>
                    }
                }
            </div>
        </div>    </div>*@
